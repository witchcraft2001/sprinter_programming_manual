# 2.2 DCP — динамический декодер портов

> **Навигация:** [← 2.1 Окна памяти](01_windows.md) | [Оглавление](../README.md) | [2.3 Кэш →](03_cache.md)

---

## Что такое DCP

**DCP** (Dynamic Configuration Port) — ядро системы декодирования портов I/O в Sprinter. Реализован в FPGA ACEX 1K30 как **256-элементная LUT-память шириной 16 бит** (файл `DCP.MIF`).

При каждой операции `IN A, (port)` или `OUT (port), A`:

1. Адрес порта A[7..0] — ключ в таблице DCP.
2. DCP возвращает 16-битное слово `MEM.q[15..0]`.
3. Старшие биты `q[15..12]` = `TYPE[3..0]` — тип функции.
4. Биты `q[14..12]` определяют тайминги (wait states).
5. Биты `q[11..0]` = `MA[11..0]` — управляющие сигналы для MAX7000.

```
DCP.MIF: 256 × 16 бит
─────────────────────────
Addr (8 бит) = номер порта Z80

  bit 15─12   TYPE[3..0]   тип операции
  bit 14─12   wait state selector
  bit 11─ 0   MA[11..0]    управление MAX7000, HDD, CMOS
```

---

## Поле TYPE[3..0]

| TYPE (биты 15–12) | Значение | Описание |
|-------------------|---------|----------|
| `0b0000` = `0` | Нет функции | Порт не декодирован, нет реакции |
| `0b0001` = `1` | HDD данные | Данные IDE (`INIR`/`OTIR`) |
| `0b0010` = `2` | Запись | Запись в системный регистр |
| `0b0011` = `3` | Чтение AY/COVOX | Возврат данных |
| `0b0100` = `4` | Клавиатура | Чтение клавиш |
| `0b0101` = `5` | Kempston Mouse | Чтение мыши |
| `0b0110` = `6` | Видео | Чтение видеорегистра |
| `0b0111` = `7` | MAX7000 | FDD/HDD регистры/CMOS через MAX7000 |
| `0b1100`–`0b1111` = `C–F` | Системные | Переключение страниц памяти, режимов |

---

## Wait States (тайминги доступа)

Количество дополнительных тактов ожидания определяется комбинацией `(TURBO, MEM.q[14..12])`:

```
CASE (TURBO, TYPE[2..0]) IS
    Нормальный режим (TURBO=0):
    TYPE=0 → W_TAB = 2   (RAM)
    TYPE=1 → W_TAB = 2   (RAM расширенная)
    TYPE=2 → W_TAB = 1
    TYPE=3 → W_TAB = 1
    TYPE=4 → W_TAB = 1
    TYPE=5 → W_TAB = 2   (ROM)
    TYPE=6 → W_TAB = 7   (VRAM с видеоконфликтами)
    TYPE=7 → W_TAB = 10  (MAX7000 / IDE регистры)

    Turbo режим (TURBO=1):
    TYPE=0 → W_TAB = 2
    TYPE=1 → W_TAB = 2
    TYPE=2 → W_TAB = 4   (ROM в turbo)
    TYPE=3 → W_TAB = 4
    TYPE=4 → W_TAB = 7
    TYPE=5 → W_TAB = 7
    TYPE=6 → W_TAB = 7
    TYPE=7 → W_TAB = 10
```

**Специальный случай — IDE данные (порты `#0050`/`#0150`):**
```
MEM.q[11..8] == 0 → HDD_W = 4  (данные)
иначе              → HDD_W = 10 (регистры)
```

### Сводная таблица wait states

| Тип | Нормальный (7 МГц) | Turbo (21 МГц) |
|-----|---------------------|-----------------|
| RAM | 2 | 2 |
| ROM/Flash | 2 | 4 |
| VRAM | 7 | 7 |
| MAX7000 / FDD | 10 | 10 |
| IDE регистры | 10 | 10 |
| IDE данные | 4 | 4 |

---

## Содержимое DCP.MIF (ключевые записи)

Файл `DCP.MIF` — это исходная таблица декодирования. Формат: `адрес : значение`.

```
[0..FF] : 1000     ; по умолчанию — нет функции (TYPE=1, нет реакции)

0       : 1040     ; DCP PAGE (системный)

10      :          ; FDD / периферия через MAX7000
    7018  ; RD WG93 порт 1F/0F
    7019  ; RD WG93 порт 3F
    701A  ; RD WG93 порт 5F
    701B  ; RD WG93 порт 7F
    7017  ; WR_PDOS FF
    701F  ; RD_KEYS / WR_A20

1C      : 71D8     ; CMOS_DAT_RD
1D      : 70D4     ; CMOS_ADR_WR
1E      : 71D4     ; CMOS_DAT_WR

20      :          ; IDE CS1 и CS3 порты
    60A8  ; HD_CS1 (порт 1F0)
    62A8  ; HD_CS1 (порт 1F1)
    ...
    6DA8  ; HD_CS3 (порт 3F6)
    6FA8  ; HD_CS3 (порт 3F7)
    7060  ; Set HDD1
    7064  ; Set HDD2
    7120  ; Set 320 Lines
    7124  ; Set 312 Lines
    7160  ; Soft Reset

30      :          ; ISA слот
    7000  ; slot 1 ports
    7001  ; slot 2 ports
    7002  ; slot 1 mem
    7003  ; slot 2 mem

40      : 4000     ; клавиатура (TYPE=4)
52      : 3000     ; AY_D READ (TYPE=3)
58      : 5000     ; Kempston Mouse (TYPE=5)

[80..FF]: C000     ; Системные порты (TYPE=C), банкинг

88      : 2000     ; COVOX данные
89      : 2000     ; COVOX режим

8C      : 3000     ; AY_D READ
8D      : 2000     ; AY_A WRITE
8E      : 2000     ; AY_D WRITE
8F      : 2000     ; ROM_WRITE

90..9F  :          ; RAM PAGES (алиасы страниц)
    3030 3031 2032 2033 ...

B0..BF  :          ; RAM PAGES (алиасы)
C0..CF  : 2000     ; SYS PORTS (копии системных портов)
D0..DF  :          ; RAM PAGES
E0..EF  :          ; ROM PAGES
F0..FF  :          ; RAM PAGES
```

---

## Системные регистры (запись в диапазон #C0–#CF)

Запись в порты `#C0`–`#CF` управляет внутренними регистрами DCP. В DCP.TDF задекодировано через `DCPP[] == B"1100XXXX"`:

| DCPP-паттерн | Порт(ы) | Регистр |
|-------------|---------|---------|
| `B"1100X000"` | `#C0`, `#C8` | `SC[7..0]` — конфигурация режима |
| `B"1100X001"` | `#C1`, `#C9` | `PN[7..0]` — нумерация страниц |
| `B"1100X010"` | `#C2`, `#CA` | `BORDER` — бордюр |
| `B"1100X011"` | `#C3`, `#CB` | `ALL_MODE` — графический режим |
| `B"1100X101"` | `#C5`, `#CD` | `RGMOD` — double buffer, screen |
| `B"11001011"` | `#CB` | `HOLD` — акселератор |

### Регистр SC (System Configuration)

Запись в `#C0` (или `#C8`):

| Бит | Имя | Назначение |
|-----|-----|-----------|
| 0 | SC0 | ZX-режим: 1 = ZX Spectrum совместимость |
| 1 | SC1 | ROM16: 1 = 16-битная ROM |
| 2 | SC2 | NMI разрешение |
| 3 | SC3 | Дополнительный режим |
| 4 | SC4 | `BLK_R` — доступ к расширенной памяти |

```asm
; Включить нативный режим Sprinter (SC0=0)
        ld      a, SYS_PORT_OFF ; #3C
        out     (#3C), a

; Активировать расширенный блок памяти (SC4=1)
SYS_PORT_ON     EQU     #7C
        ld      a, SYS_PORT_ON
        out     (SYS_PORT_ON), a
```

### Регистр CNF (Configuration)

Управляет глобальными флагами:

| Бит | Имя | Назначение |
|-----|-----|-----------|
| 3 | CNF3 | Scorpion режим |
| 4 | CNF4 | ROM16 on/off |
| 5 | CNF5 | Сброс PN5 |
| 6 | CNF6 | Scorpion-OFF |
| 7 | CNF7 | Pentagon-512 |

---

## Как DCPP управляет работой ACEX

После декодирования порта `DCPP[7..0]` остаётся в регистре до конца цикла I/O и используется всеми блоками ACEX:

```
Видео-контроллер проверяет DCPP для RGMOD, BORDER, ALL_MODE
COVOX-блок реагирует на DCPP == #88 (данные) и #89 (режим)
AY-синтезатор смотрит на DCPP == #8D (адрес) и #8E (данные)
ROM_RG активируется при DCPP == #8F
ISA контроллер: DCPP == B"1111XXXX"
Банкинг: DCPP == B"1100XXXX" → запись в SC/PN/BORDER...
```

---

## Изменение DCP таблицы

**В рабочем коде изменять DCP.MIF нельзя** — это содержимое FPGA, зашивается при компиляции. Однако система предусматривает динамическую замену таблицы через порт `#00` (DCP PAGE):

```asm
; Порт #00 — запись страницы в DCP (специальная функция DCP PAGE)
; Используется BIOS при инициализации
; Не предназначен для использования из пользовательского кода
```

---

## Практические выводы для программиста

1. **Порт = функция**: если порт есть в DCP.MIF с ненулевым TYPE — он работает; иначе — нет реакции.
2. **Wait states зависят от TYPE**: RAM быстрее ROM, ROM быстрее VRAM.
3. **Системные порты `#C0`–`#CF`**: управляют режимами чипа — не используйте их случайно.
4. **IDE**: запись и чтение — разные физические порты (`HDW_xxx` vs `HDR_xxx`).
5. **ISA**: адрес порта формируется через `ISA_PORT` регистр, не напрямую.

---

## Ключевые моменты

> - DCP — 256×16-бит таблица в FPGA; поле TYPE[3..0] (биты 15–12) определяет функцию порта.
> - Wait states заданы в таблице: RAM=2, ROM=2/4, VRAM=7, MAX7000/FDD=10, IDE регистры=10, IDE данные=4.
> - Диапазон `#C0`–`#CF` — системные регистры (SC, PN, BORDER, ALL_MODE, RGMOD).
> - Изменить DCP из программы нельзя — это прошивка FPGA.

---

## Ссылки и источники {#ссылки}

| Источник | Путь / URL |
|---------|-----------|
| DCP.MIF таблица | `sprinter_ai_doc/sp-altera-src-2025-11-30/altera/acex/K30/DCP.MIF` |
| DCP логика | `sp-altera-src-2025-11-30/altera/acex/K30/DCP.TDF` |
| ACEX top-level | `sp-altera-src-2025-11-30/altera/acex/K30/SP2_ACEX.TDF` |
